---
title: Next.js Integration
description: Route Handlers, Edge Middleware, and Server Component helpers for Next.js App Router.
---

# Next.js Integration

The `@bolkauth/nextjs` package provides first-class support for Next.js 13+ App Router, Edge Middleware, and Server Components.

---

## 🛠️ 1. Dynamic Route Handler

Catch all authentication API routes with a single catch-all handler file at `app/api/auth/[...bolkauth]/route.ts`:

```ts
import { bolkAuthHandler } from "@bolkauth/nextjs";
import { auth } from "@/lib/auth";

const handlers = bolkAuthHandler(auth);

export const GET = handlers.GET;
export const POST = handlers.POST;
export const PATCH = handlers.PATCH;
export const DELETE = handlers.DELETE;
```

---

## 🛡️ 2. Edge Middleware

Protect public and private routes using `bolkAuthMiddleware` in `middleware.ts`:

```ts
import { bolkAuthMiddleware } from "@bolkauth/nextjs";
import { auth } from "@/lib/auth";

export default bolkAuthMiddleware(auth, {
  signInUrl: "/sign-in",
  onboardingUrl: "/onboarding",
  publicRoutes: ["/", "/sign-in", "/sign-up", "/api/auth"],
});

export const config = {
  matcher: ["/((?!.*\\..*|_next).*)", "/", "/(api|trpc)(.*)"],
};
```

---

## 🖥️ 3. Server Component Helpers

Retrieve authenticated user sessions directly inside Server Components and Server Actions:

```tsx
import { createServerHelpers } from "@bolkauth/nextjs";
import { auth } from "@/lib/auth";

const { getSession, getUser, requireAuth } = createServerHelpers(auth);

export default async function DashboardPage() {
  const user = await requireAuth();

  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Welcome back, {user.name || user.email}!</h1>
      <p className="text-gray-500">User ID: {user.id}</p>
    </div>
  );
}
```
